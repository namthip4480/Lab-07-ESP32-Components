# Link Repo lab

https://github.com/namthip4480/67030115-Lab07

---

# 🎯 การอภิปราย: การจัดการ Component ในระบบ Embedded / Software Project

##  1. เมื่อไหร่ควรสร้าง Component ใหม่ แทนที่จะเขียนโค้ดใน Main Application
- เมื่อฟังก์ชันนั้น **สามารถแยกเป็นหน่วยอิสระ** ที่สามารถนำกลับมาใช้ซ้ำ (reusable) ได้  
- เมื่อโค้ดมี **ความซับซ้อนสูง** และต้องการ **แยกความรับผิดชอบ (Separation of Concerns)**  
- เมื่อทีมต้องการให้ **หลายคนพัฒนา/ทดสอบพร้อมกันได้โดยไม่รบกัน**  
- เมื่อฟังก์ชันต้องการ **unit test แยกเฉพาะ**  
- เพื่อให้ **โครงสร้างโค้ดชัดเจนและดูแลรักษาง่ายขึ้น**

---

##  2. ความแตกต่างระหว่าง `REQUIRES` และ `PRIV_REQUIRES` ส่งผลต่อการ Compile อย่างไร
| รายการ | REQUIRES | PRIV_REQUIRES |
|--------|-----------|---------------|
| การเชื่อมโยง (Linking) | นำ dependency มาสู่ **component ปัจจุบันและ component ที่เรียกใช้มัน** | ใช้ dependency เฉพาะภายใน component เท่านั้น |
| การมองเห็น (Visibility) | dependency ถูกส่งต่อ (propagate) | dependency ไม่ถูกส่งต่อ |
| ผลต่อการ compile | ขยายกราฟ dependency มากขึ้น อาจ compile นานขึ้น | จำกัดขอบเขต dependency ทำให้ compile เร็วและชัดเจนขึ้น |

---

##  3. การเลือกใช้ Local Component vs Managed Component
| ปัจจัย | Local Component | Managed Component |
|---------|----------------|------------------|
| ที่มา | อยู่ใน project เดียวกัน | มาจาก package registry หรือ repo ภายนอก |
| การควบคุมโค้ด | แก้ไขได้โดยตรง | มักเป็น read-only หรือ version-controlled |
| การอัปเดต | ต้องจัดการเอง | สามารถอัปเดตผ่าน version/tag |
| ความเสี่ยง | ปลอดภัยกว่าในเชิงควบคุม | เสี่ยง dependency conflict ถ้า version ไม่ตรง |

---

##  4. ในทีมงานขนาดใหญ่ จะจัดการ Component Dependencies อย่างไรให้มีประสิทธิภาพ
- ใช้ **version control ที่ชัดเจน** เช่น semantic versioning (`v1.2.3`)  
- แยก **component registry หรือ repository** สำหรับจัดการ dependencies  
- ใช้ **CI/CD** ตรวจสอบการ build แต่ละ component แบบอัตโนมัติ  
- กำหนด **dependency graph ที่โปร่งใส** (เช่น ผ่าน manifest หรือ component.yaml)  
- ใช้ **code review + dependency approval** เพื่อควบคุมคุณภาพก่อน merge


---

##  5. การทดสอบ Component ที่มีการพึ่งพา Hardware ทำได้อย่างไร
- ใช้ **Mock หรือ Stub** จำลองการทำงานของ hardware interface  
- สร้าง **Hardware Abstraction Layer (HAL)** เพื่อแยกโค้ด logic ออกจาก hardware จริง  
- ใช้ **simulation / emulator** หาก platform รองรับ (เช่น QEMU)  
- แยกการทดสอบออกเป็น 2 ส่วน:
  - **Unit Test** → ใช้ mock / stub  
  - **Integration Test / HIL (Hardware-in-the-Loop)** → ทดสอบบนบอร์ดจริง  
- ใช้ **CI pipeline** เชื่อมกับ hardware test farm (อัตโนมัติรันบนบอร์ดจริง)

---
